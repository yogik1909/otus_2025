/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
        label 'Мастер'
    }
    stages {
        stage('Build') {
            steps {
                bat 'echo test'
            }
        }
    }
}