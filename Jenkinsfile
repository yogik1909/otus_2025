/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
       any
    }
    stages {
        stage('Build') {
            steps {
                bat 'echo test'
            }
        }
    }
}