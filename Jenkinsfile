/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
        lable 'Мастер'
    }
    stages {
        stage('Build') {
            steps {
                bat 'echo test
            }
        }
    }
}